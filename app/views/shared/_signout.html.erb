<% if user_signed_in? %>
        <%= form_tag(destroy_user_session_path, method: :delete, class: "d-flex justify-content-center") do %>
          <%= submit_tag("Sign Out", class: "btn btn-danger") %>
        <% end %>
    <% end %>